<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Container Instance</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/createInstance.js"></script>
</head>
<body class="bg-gray-100">

    <div class="container mx-auto p-6">
        <header class="mb-8">
            <h1 class="text-2xl font-bold">Create New Container Instance</h1>
            <a href="/dashboard" class="text-blue-500 hover:underline">Back to Dashboard</a>
        </header>

        <form id="create-form" class="space-y-4 bg-white p-6 rounded-lg shadow-md">
            <div>
                <label for="image" class="block text-sm font-medium text-gray-700">Image</label>
                <input type="text" id="image" name="Image" required class="mt-1 p-2 border border-gray-300 rounded-lg w-full">
            </div>

            <div>
                <label for="id" class="block text-sm font-medium text-gray-700">ID</label>
                <input type="text" id="id" name="Id" required class="mt-1 p-2 border border-gray-300 rounded-lg w-full">
            </div>

            <div>
                <label for="memory" class="block text-sm font-medium text-gray-700">Memory</label>
                <input type="number" id="memory" name="Memory" required class="mt-1 p-2 border border-gray-300 rounded-lg w-full">
            </div>

            <div>
                <label for="cpu" class="block text-sm font-medium text-gray-700">CPU</label>
                <input type="number" id="cpu" name="Cpu" required class="mt-1 p-2 border border-gray-300 rounded-lg w-full">
            </div>

            <div>
                <label for="env" class="block text-sm font-medium text-gray-700">Environment Variables</label>
                <textarea id="env" name="Env" class="mt-1 p-2 border border-gray-300 rounded-lg w-full h-32"></textarea>
            </div>

            <button type="submit" class="w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Create Instance</button>
        </form>
    </div>

    <script src="/createInstance.js"></script>
</body>
</html>
